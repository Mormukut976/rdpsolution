{
  "name": "openremote",
  "version": "0.1.0",
  "private": true,
  "description": "Free and open-source remote desktop and application delivery platform.",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "init": "node scripts/init.mjs",
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "check": "node --test && node --check public/app.js"
  },
  "license": "AGPL-3.0-only"
}
